import styled from "styled-components";

export const PageContentRoot = styled.div`
    max-width: var(--page-max-width);
    display: flex;
    flex-direction: column;
    flex: 1;
    margin: 0 auto;
    min-height: 100vh;
    padding: 4rem var(--horizontal-page-padding) 4rem;
`;
